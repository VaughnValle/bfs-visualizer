import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import extractMatchingColor from '../src/helpers/ExtractMatchingColor';
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
